const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "nodetshego",
      password: "!HFLMzii9PPM_kT",
      database: "nodedb_test",
    },
    listPerPage: 10,
  };
  module.exports = config;